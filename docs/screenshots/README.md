# Screenshots

Part IV evidence goes here:

- `01-before-saving.png` — title screen showing "Supabase connected" beside the empty `game_runs` table
- `02-save-success.png` — after-action report showing "Saved to Supabase"
- `03-row-in-database.png` — the new row in the Supabase Table Editor or SQL Editor
- `04-retrieved-data.png` — the Top runs board after pressing "Load from database"
- `05-connection-code.png` — the `DB` block and the `saveRun` function in `script.js`
